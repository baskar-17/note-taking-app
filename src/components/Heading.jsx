const react = require("react")

function Heading () {
return(
    <header>
    <h1>NoteDown</h1>

    <button className="primary-button" id="primaryButton">+ Add New</button>
    </header>
);
}

export default Heading